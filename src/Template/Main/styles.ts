import styled from 'styled-components';

export const Container = styled.main`
  padding: 20px 10px;
  background-color: #f6f6f6;
  min-height: calc(100vh - (70px + (24px * 4)));
`;
// FIXME: colocar variaveis de tamanho no theme provider